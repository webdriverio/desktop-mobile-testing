backend:
  api: "Backend API patterns and conventions"
  migrations: "Database migration standards"
  models: "Data model conventions"
  queries: "Query patterns and best practices"

frontend:
  accessibility: "Accessibility requirements and patterns"
  components: "UI component conventions"
  css: "CSS/styling standards"
  responsive: "Responsive design patterns"

global:
  binary-discovery: "Binary discovery chain, auto-install, and Result type pattern"
  browser-api-injection: "Framework API namespace on browser object and mock lifecycle"
  cross-platform: "Cross-platform process handling for Windows, macOS, Linux"
  diagnostics: "Environment diagnostics system for pre-flight checks"
  driver-pool: "Driver pool for managing multiple concurrent driver instances"
  driver-process-lifecycle: "Spawning, readiness detection, log forwarding, graceful shutdown"
  error-messages: "Actionable error messages with fix steps, launcher error conventions"
  mock-architecture: "Inner/outer mock pattern spanning app and test process boundaries"
  mock-store: "MockStore singleton for tracking active mocks and bulk operations"
  mock-wrapper: "Wrapper pattern for vitest mock compatibility across processes"
  native-spy: "@wdio/native-spy package for vitest-compatible mocking in app contexts"
  package-entry-points: "Required exports from service package index.ts"
  port-allocation: "Dynamic port allocation with PortManager for parallel/multiremote"
  service-architecture: "Launcher/worker service split pattern and WDIO hook responsibilities"

testing:
  test-writing: "Test structure and conventions"
